# Vite API URL Configuration
# This environment variable is used by the Vite dev server proxy to forward API requests
# to the backend service.

# Development (DevContainer):
# When running in a DevContainer, use host.docker.internal to reach the host machine
VITE_API_URL=http://host.docker.internal:8080/HotelReservation-war

# Development (docker-compose):
# When running in docker-compose, use the service name to reach the backend container
# VITE_API_URL=http://hotel-api-rest:8080/HotelReservation-war

# Production:
# When building for production, set this to your production API URL
# VITE_API_URL=https://api.example.com/HotelReservation-war
